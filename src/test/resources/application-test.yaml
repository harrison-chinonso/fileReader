spring:
  application:
    name: FileReader
  datasource:
    url: jdbc:h2:mem:REQ_LIMIT_TEST_DB;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
